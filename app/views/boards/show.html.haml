.container
  %h2.heading= @board.title
  - @tasks.each do |task|
    = link_to board_task_path(board_id: @board.id, id: task.id) do
      .card
        .card_title
          = task.title
        .task_content
          = task.description
  = link_to '＋Add new task', new_board_task_path(@board), class: 'new_board'
